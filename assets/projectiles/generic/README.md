# Generic projectile images

Expected optional fallback images:

- `bullet.png`
- `rocket.png`

These are used only when no class-specific projectile image is available. Each is a single static transparent PNG, not an animation.
