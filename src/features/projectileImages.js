// Compatibility layer retained for the existing feature import chain.
// Projectile visuals are now procedural only; there is no projectile asset pipeline.
export { Game, UI } from './upgradeIconCards.js';
